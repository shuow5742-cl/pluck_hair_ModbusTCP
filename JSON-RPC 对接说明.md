# JSON-RPC 对接说明（供算法主程序对接 `ModbusTCP_auto_runner_rpc_state.py`）

## 1. 目标

算法主程序通过 JSON-RPC 与自动通信脚本 `ModbusTCP_auto_runner_rpc_state.py` 对接。
 PLC 侧通信逻辑、寄存器时序、首拍/复拍判断、结果状态清零、固定 Z 值处理，全部由自动通信脚本负责。
 算法主程序只需要做一件事：

**收到一次“请求异物坐标”后，返回 `x / y / u / state`。**

------

## 2. 职责边界

### 2.1 算法主程序负责

1. 提供 JSON-RPC 服务
2. 实现 `health` 方法
3. 实现 `detect_once` 方法
4. 在 `detect_once` 被调用时：
   - 拍照
   - 图像处理
   - 计算 `x / y / u`
   - 判断 `state`
   - 返回 JSON-RPC 结果

### 2.2 算法主程序不负责

1. 不负责 Modbus TCP 通信
2. 不负责区分首拍还是复拍
3. 不负责 PLC 寄存器写入
4. 不负责结果状态 `1/2` 的翻译
5. 不负责挑取次数写入 PLC
6. 不负责固定 `Z` 值处理
7. 不负责 `no_target` 时坐标清零
8. 不负责 PLC ACK、复拍触发、流程状态机

------

## 3. JSON-RPC 服务信息

### 3.1 服务地址

- Host：`127.0.0.1`
- Port：`18080`
- Path：`/jsonrpc`

完整地址：

```
http://127.0.0.1:18080/jsonrpc
```

### 3.2 协议

- JSON-RPC 2.0
- HTTP POST

------

## 4. 必须实现的方法

## 4.1 `health`

### 作用

供自动通信脚本检查主程序服务是否在线。

### 请求示例

```
{
  "jsonrpc": "2.0",
  "method": "health",
  "params": {},
  "id": 1
}
```

### 返回示例

```
{
  "jsonrpc": "2.0",
  "result": {
    "ok": true
  },
  "id": 1
}
```

### 字段类型

- `ok`：`bool`

------

## 4.2 `detect_once`

### 作用

自动通信脚本每次需要异物结果时，会调用一次 `detect_once`。
 算法主程序不需要关心这是首拍还是复拍，只要收到请求就返回当前结果。

### 请求示例

```
{
  "jsonrpc": "2.0",
  "method": "detect_once",
  "params": {},
  "id": 2
}
```

### 请求字段说明

本次请求不要求主程序解析业务参数。
 主程序只要实现 `detect_once` 并返回结果即可。

### `id` 字段类型

- `id`：`int` 或 `str`
- 主程序返回时应原样带回

------

## 5. `detect_once` 返回格式

### 有目标时返回示例

```
{
  "jsonrpc": "2.0",
  "result": {
    "x": 50.021,
    "y": 35.926,
    "u": 39.612,
    "state": "new_target"
  },
  "id": 2
}
```

### 无目标时返回示例

```
{
  "jsonrpc": "2.0",
  "result": {
    "state": "no_target"
  },
  "id": 2
}
```

------

## 6. 返回字段及数据类型说明

### 6.1 必要返回字段

#### `state`

- 类型：`string`
- 必填：是

允许值只有 4 个：

- `new_target`
- `retry_1`
- `retry_2`
- `no_target`

#### `x`

- 类型：`number`
- 推荐：`float`
- 必填：当 `state != "no_target"` 时必填

#### `y`

- 类型：`number`
- 推荐：`float`
- 必填：当 `state != "no_target"` 时必填

#### `u`

- 类型：`number`
- 推荐：`float`
- 必填：当 `state != "no_target"` 时必填

### 6.2 无目标时的返回规则

当：

```
state = "no_target"
```

主程序可以只返回：

```
{
  "state": "no_target"
}
```

也就是说，无目标时可以不返回 `x / y / u`。

------

## 7. `state` 的业务含义

### `new_target`

表示：

- 当前返回的是一个新的异物坐标
- 这是该异物的首次抓取坐标

### `retry_1`

表示：

- 当前返回的是第一次复抓坐标

### `retry_2`

表示：

- 当前返回的是第二次复抓坐标
- 也就是第三次抓取

### `no_target`

表示：

- 当前视野已经没有异物
- 不再返回有效坐标

------

## 8. 自动通信脚本如何解释这些返回值

以下逻辑由 `ModbusTCP_auto_runner_rpc_state.py` 自动处理，算法主程序无需关心。

### 当 `state = "new_target"`

自动通信脚本会：

- 向 PLC 发送有效坐标
- 挑取次数写为 `0`
- 结果状态写为 `1`
- 固定 `Z = 50.16`

### 当 `state = "retry_1"`

自动通信脚本会：

- 向 PLC 发送有效坐标
- 挑取次数写为 `1`
- 结果状态写为 `1`
- 固定 `Z = 50.16`

### 当 `state = "retry_2"`

自动通信脚本会：

- 向 PLC 发送有效坐标
- 挑取次数写为 `2`
- 结果状态写为 `1`
- 固定 `Z = 50.16`

### 当 `state = "no_target"`

自动通信脚本会：

- 向 PLC 发送结果状态 `2`
- 自动把 `X / Y / Z / U` 全部发送为 `0`

------

## 9. 主程序实现要求

### 9.1 字段名必须固定

返回字段名必须使用：

- `x`
- `y`
- `u`
- `state`

不要改成其他名字，例如：

- `X`
- `pos_x`
- `angle_u`
- `status`

都不要使用。

### 9.2 `state` 只能返回 4 个合法字符串

只允许：

- `new_target`
- `retry_1`
- `retry_2`
- `no_target`

### 9.3 坐标类型要求

`x / y / u` 应返回数字类型。
 推荐直接返回 JSON number，例如：

```
{
  "x": 50.021,
  "y": 35.926,
  "u": 39.612
}
```

不要返回成字符串，例如：

```
{
  "x": "50.021",
  "y": "35.926",
  "u": "39.612"
}
```

### 9.4 `no_target` 时不要伪造坐标

当已经没有异物时，建议直接返回：

```
{
  "state": "no_target"
}
```

不要再额外构造无意义坐标。

------

## 10. 最小可用返回示例

### 新目标

```
{
  "jsonrpc": "2.0",
  "result": {
    "x": 50.021,
    "y": 35.926,
    "u": 39.612,
    "state": "new_target"
  },
  "id": 2
}
```

### 第一次复抓

```
{
  "jsonrpc": "2.0",
  "result": {
    "x": 49.800,
    "y": 35.500,
    "u": 39.612,
    "state": "retry_1"
  },
  "id": 2
}
```

### 第二次复抓

```
{
  "jsonrpc": "2.0",
  "result": {
    "x": 49.600,
    "y": 35.200,
    "u": 39.612,
    "state": "retry_2"
  },
  "id": 2
}
```

### 当前无异物

```
{
  "jsonrpc": "2.0",
  "result": {
    "state": "no_target"
  },
  "id": 2
}
```

------

## 11. 错误返回建议

如果主程序内部异常，建议返回 JSON-RPC 错误响应，而不是返回格式错误的 `result`。

示例：

```
{
  "jsonrpc": "2.0",
  "error": {
    "code": -32000,
    "message": "detect failed"
  },
  "id": 2
}
```

------

## 12. 联调前自检清单

主程序同事在提交前，建议先确认：

1. `health` 可正常返回 `{ "ok": true }`
2. `detect_once` 可正常返回 JSON-RPC 2.0 格式
3. `state` 只会返回 4 个允许值之一
4. 有目标时会返回 `x / y / u`
5. `no_target` 时可以只返回 `state`
6. `x / y / u` 返回的是数字，不是字符串
7. `id` 会原样回传

------

## 13. 一句话总结

主程序只需要实现一个最简单的能力：

**收到一次 `detect_once` 调用后，返回 `x / y / u / state`；其他所有 PLC 通信、流程控制、固定 Z、重试次数翻译，都由自动通信脚本负责。**